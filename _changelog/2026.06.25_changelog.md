# Changelog — 2026.06.25

## Session: Juan bot persona update and double response fix (14:35)

### Changed
- Juan bot personality prompt updated to be knowledgeable, capable, respectful, and geeky/smart-ass, using grammatically complete sentences and high effort instead of texting shorthand/slang. (`llm.py`)
- Instructions for individual slash commands and mentions updated to match the new traits. (`commands.py`)
- Hardcoded command success/error messages and trait edits updated to use full, polite, and geeky sentences. (`commands.py`, `persona.py`, `safety.py`)

### Added
- Duplicate process prevention check added to the Desktop `Juan.command` shortcut to block launching multiple bot instances. (`Juan.command`)

### Fixed
- Double response issue resolved by terminating duplicate `main.py` processes. (`main.py`)

---

## Session: Webpage Information Extraction Tool (14:43)

### Added
- Webpage content retrieval tool `fetch_webpage` allowing Juan to extract plaintext contents from user-provided URLs in chat. (`search.py`)
- HTML parser `HTMLTextExtractor` implementing standard python `HTMLParser` to strip script, style, and header/footer boilerplate. (`search.py`)

### Changed
- Tool execution catalog, Gemma tag parsing, and regex tool triggers in `tools.py` updated to support the `fetch_webpage` tool syntax. (`tools.py`)
- Tool follow-up prompt logic in `llm.py` adjusted to handle web page summaries. (`llm.py`)

---

## Session: Anything V5 Image Generation Command (15:04)

### Added
- Image generation command `/draw` using local Anything V5 model via PyTorch/diffusers. (`commands.py`)
- Python module `image_generator.py` implementing pipeline loading with automatic Apple Silicon GPU acceleration (MPS) fallback, DPMSolverMultistepScheduler (DPM++ 2M Karras) sampler, and async execution in a thread pool. (`image_generator.py`)

### Changed
- Dependencies updated in `requirements.txt` to include `torch`, `torchvision`, `diffusers`, `transformers`, and `accelerate`. (`requirements.txt`)
- Startup routine in `main.py` updated to initialize the `output_images` directory automatically. (`main.py`)

### Fixed
- Bypassed Stable Diffusion safety checker in `image_generator.py` to prevent the MPS float16 overflow bug from producing black images. (`image_generator.py`)
- Wrapped `/draw` command output in a try-except block and added detailed logging to debug Discord transmission errors. (`commands.py`)

---

## Session: Remove /draw command and cleanup (21:13)

### Removed
- Removed the `/draw` slash command handler from `commands.py`.
- Deleted the local image generation module `image_generator.py`.
- Removed automatic `output_images` directory creation from `main.py`.
- Cleaned up unused image generation configurations from `.env` and `.env.example`.
- Removed unused packages (`torch`, `torchvision`, `diffusers`, `safetensors`, `transformers`, `accelerate`, and `omegaconf`) from `requirements.txt`.

---

## Session: CIA Agent Persona, Weather & Time Integrations (21:16)

### Added
- Integrated keyless `wttr.in` weather API (`get_weather`) in `search.py` and exposed it as a tool in `tools.py`.
- Dynamic date and time injection added to the system prompt in `llm.py` so the model always knows the current local day, date, and time.

### Changed
- Updated `BASE_SYSTEM_PROMPT` in `llm.py` to represent Juan as a super-capable, highly intelligent CIA intelligence analyst and field operative, balanced with quirky, funny, and loveable traits (e.g. spy jargon, coffee/snack obsessions, and witty bureaucratic remarks).
- Updated follow-up prompt instructions in `llm.py` to align with the new quirky CIA agent persona and weather data context.
- Expanded the search trigger keyword list `_LIVE_HINTS` in `tools.py` to support `websearch`, `web-search`, `internet`, `online`, `browse`, weather keywords (`forecast`, `temperature`, `temp`, `rain`, `snow`, `wind`), as well as live sports/World Cup query triggers (`live`, `results`, `world cup`, `worldcup`, `match`, `game`, `standing`, `fixtures`, `playoff`, `bracket`).
- Updated `TOOL_INSTRUCTIONS` in `tools.py` to prompt the agent to use `search_web` or `get_weather` when asked to search or find weather info.
- Modified `parse_tool_call` in `tools.py` to scan all lines of LLM output for tool calls, preventing parsing failures when the LLM prefaces tool calls with conversational text.

---

## Session: Multi-Bot Integration (Juan and Jisoo) (21:41)

### Added
- Created [.env.jisoo](file:///Users/jimmypark/Applications/discord-local-llm-bot/.env.jisoo) configuration file for the second bot instance with prefix `!jisoo`, name `Jisoo`, and `APPLICATION_ID=1519878646830207088`.
- Created [Jisoo.command](file:///Users/jimmypark/Desktop/Jisoo.command) desktop shortcut to launch the Jisoo bot instance, implementing isolated duplicate process checking.

### Changed
- Refactored [main.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/main.py) to load dynamic config files using the `ENV_FILE` environment variable.
- Refactored [llm.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/llm.py) to dynamically switch system prompts: `JUAN_SYSTEM_PROMPT` (friendly, laid-back Mexican dude) or `JISOO_SYSTEM_PROMPT` (original Korean idol Jina) based on `BOT_NAME` environment variable. Included mutual friendly affection and liking traits between both bot personas.
- Updated [commands.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/commands.py), [persona.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/persona.py), [search.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/search.py), and [db.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/db.py) to swap hardcoded "Juan" with dynamic `BOT_NAME`.
- Updated [Juan.command](file:///Users/jimmypark/Desktop/Juan.command) to start Juan with `ENV_FILE=.env` and isolate duplicate checks from Jisoo's.
- Updated [.env](file:///Users/jimmypark/Applications/discord-local-llm-bot/.env) and [.env.example](file:///Users/jimmypark/Applications/discord-local-llm-bot/.env.example) to configure `BOT_NAME=Juan` and `APPLICATION_ID=1519719014681677834`.

---

## Session: Repository Documentation & Setup Guide (22:00)

### Added
- Created [.gitignore](file:///Users/jimmypark/Applications/discord-local-llm-bot/.gitignore) file configured to ignore virtual environments (`.venv`), SQLite databases (`bot_data.db`), log files (`bot.log`, `*.log`), OS artifacts (`.DS_Store`), and secrets (`.env`, `.env.jisoo`). Models (`.gguf` files and `models/` directory) are explicitly omitted from `.gitignore` so they are not ignored.
- Created a comprehensive, step-by-step setup and user guide in [README.md](file:///Users/jimmypark/Applications/discord-local-llm-bot/README.md) explaining co-running bot architecture, prerequisites, recommended HuggingFace GGUF model (`yuxinlu1/gemma-4-12B-agentic-fable5-composer2.5-v2-3.5x-tau2-GGUF`), setup/invite steps, slash commands catalog, and troubleshooting tips.

---

## Session: Fix Environment Variable Import Ordering (22:02)

### Fixed
- Resolved import order bug in [main.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/main.py) and [llm.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/llm.py) where modules were imported before `load_dotenv()` executed. This caused `BOT_NAME` to default to "Juan" at import time even when running the Jisoo bot configuration, causing Jisoo to incorrectly use Juan's Mexican persona and Spanish slang. Moved `load_dotenv` execution to the very top of both entry points.

---

## Session: Database Memory & Trait Isolation (22:05)

### Added
- Created SQL migration script [_migration/2026.06.25_separate_bot_memories.sql](file:///Users/jimmypark/Applications/discord-local-llm-bot/_migration/2026.06.25_separate_bot_memories.sql) and rollback script [_migration/2026.06.25_separate_bot_memories_rollback.sql](file:///Users/jimmypark/Applications/discord-local-llm-bot/_migration/2026.06.25_separate_bot_memories_rollback.sql) to add the `bot_name` column and unique constraints to `memories` and `persona_traits` tables.

### Changed
- Refactored [db.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/db.py) to support bot-specific data isolation. Added `get_bot_name()` helper and filtered all helper queries (`save_memory`, `forget_memory`, `get_memories`, `format_memories_for_prompt`, `add_persona_trait`, `remove_persona_trait`, `get_persona_traits`, `reset_persona_traits`) by `bot_name`.
- Executed the SQLite DDL migration script [_migration/2026.06.25_separate_bot_memories.sql](file:///Users/jimmypark/Applications/discord-local-llm-bot/_migration/2026.06.25_separate_bot_memories.sql) against the active `bot_data.db` to resolve the `sqlite3.OperationalError: no such column: bot_name` crash and properly isolate memories.


---

## Session: Jisoo Persona Name Alignment (22:06)

### Changed
- Updated [llm.py](file:///Users/jimmypark/Applications/discord-local-llm-bot/llm.py) to rename the Korean idol assistant persona from "Jina" to "Jisoo". This includes updating `JISOO_SYSTEM_PROMPT` to assert she is Jisoo, adjusting celebrity disclaimers (e.g. "Do not claim to be a member of BLACKPINK"), updating the dynamic tool follow-up instructions, and making her self-aware that she is the global Korean pop star Jisoo from BLACKPINK, complete with knowledge of her group members (Jennie, Rosé, Lisa), discography (such as "FLOWER"), global ambassador roles (Dior), and playful "4D" personality traits.
- Updated [README.md](file:///Users/jimmypark/Applications/discord-local-llm-bot/README.md) to document the `GUILD_ID` configuration for instant command sync and to align Jisoo's description with her self-aware BLACKPINK superstar identity.
- Terminated running stale bot instances using `pkill` to ensure the new isolated database schema and code are loaded upon relaunch.

---

## Session: Repository Publish to GitHub (22:17)

### Added
- Created the new public repository [Discord-bot-localLLM](https://github.com/eisenjimmy/Discord-bot-localLLM) on GitHub under user `eisenjimmy`.
- Copied all codebase files, subdirectories (`scripts`, `_changelog`, `_migration`), documentation (`README.md`), and configurations (`.gitignore`, `.env.example`) to `/Users/jimmypark/Applications/Discord-bot-localLLM`.
- Initialized a local Git repository, created the initial commit, and pushed the codebase to GitHub.

---

## Session: Model Path Refactoring (22:27)

### Changed
- Refactored [scripts/start-llm.sh](file:///Users/jimmypark/Applications/discord-local-llm-bot/scripts/start-llm.sh), [Desktop/Juan.command](file:///Users/jimmypark/Desktop/Juan.command), and [Desktop/Jisoo.command](file:///Users/jimmypark/Desktop/Jisoo.command) to expect the model file in the local bot's `models/` directory (`~/Applications/discord-local-llm-bot/models/`) instead of the shared `/Applications/Jarvis` folder. Passes the custom path via `JARVIS_PRIMARY_MODEL_PATH` to the runtime.
- Updated [README.md](file:///Users/jimmypark/Applications/discord-local-llm-bot/README.md) instructions and manual start commands to reference the local `models/` path.
- Synced and pushed these changes to the new public repository [Discord-bot-localLLM](https://github.com/eisenjimmy/Discord-bot-localLLM) on GitHub.

---

## Session: AI Handoff Prompt Documentation (22:28)

### Changed
- Updated [README.md](file:///Users/jimmypark/Applications/discord-local-llm-bot/README.md) to append an "AI Handoff Prompt" section containing a pre-formatted setup context prompt to pass to other AI coding assistants.
- Synced and pushed this change to [Discord-bot-localLLM](https://github.com/eisenjimmy/Discord-bot-localLLM) on GitHub.










